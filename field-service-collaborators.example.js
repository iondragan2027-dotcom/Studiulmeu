/* ============================================
   StudiuMeu – ȘABLON colaboratori program teren
   ============================================
   Acesta e doar un EXEMPLU, sigur de urcat pe GitHub (nume fictive).

   Ca să folosești propriile date:
   1. Copiază acest fișier ca "field-service-collaborators.local.js"
      (fără ".example")
   2. Completează-l cu numele reale
   3. Fișierul "field-service-collaborators.local.js" e deja în
      .gitignore, deci nu va ajunge niciodată pe GitHub.
   ============================================ */

window.FIELD_SERVICE_COLLABORATORS_SEED = [
  'NUME PRENUME',
];
